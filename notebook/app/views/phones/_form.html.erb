<%= form_for(@phone, html: { class: "basic-grey" }) do |f| %>
  <% if @phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% @phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h1>Telefones</h1>

  <div class="field">
    <%= f.label :phone %><br>
    <%= f.text_field :phone %>
  </div>
  <div class="field">
    <%= f.label :contact_id %><br>
    <%= collection_select(:phone, :contact_id, @contacts_select_all, :id, :nome) %>
  </div>
  <div class="actions">
    <%= f.submit "Salvar", class: "button-submit" %>
    <%= link_to 'Voltar', phones_path, class: "button-back" %>
    <%= link_to  "Home", root_path, class: "button-home" %>
  </div>
<% end %>
